// import { getFirstName, isValidPassword } from "./user.js";

// test("Should return first name when given full name", () => {
//   const firstName = getFirstName("Andrew Mead");
//   expect(firstName).toBe("Andrew");
// });

// test("Should return first name when given first name", () => {
//   const firstName = getFirstName("Jen");
//   expect(firstName).toBe("Jen");
// });

// test("Should reject password shorter than 8 characters", () => {
//   const isValid = isValidPassword("123");
//   expect(isValid).toBe(false);
// });

// test("Should reject password that contains word password", () => {
//   const isValid = isValidPassword("abcPassword");
//   expect(isValid).toBe(false);
// });

// test("Should correctly validate a valide password", () => {
//   const isValid = isValidPassword("lar3acr2of1t");
//   expect(isValid).toBe(true);
// });
